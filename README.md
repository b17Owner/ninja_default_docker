# ninja_default_docker
Автоматизация развёртки django-ninja приложения

Запуск: 
  docker-compose build & docker-compose up -d
 
Параметры (файл .env):
    NAME=[название приложения]
    DB_NAME=[БД]
    DB_USER=[имя пользователя БД]
    DB_PASSWORD=[пароль БД]
